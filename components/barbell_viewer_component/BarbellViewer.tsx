import { StyleSheet, Text, View } from 'react-native'

const Barbell_Viewer = () => {
  return (
    <View style={styles.container}>
      <Text>Barbell Viewer</Text>
    </View>
  )
}

export default Barbell_Viewer

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'teal',
  },
})
